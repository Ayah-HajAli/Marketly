apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: capstone

spec:
  replicas: 1

  selector:
    matchLabels:
      app: auth-service

  template:
    metadata:
      labels:
        app: auth-service

    spec:
      imagePullSecrets:
        - name: regcred

      containers:
        - name: auth-service
          image: 571996361871.dkr.ecr.eu-north-1.amazonaws.com/capstone-auth-service:latest
          imagePullPolicy: Always

          ports:
            - containerPort: 5101

          envFrom:
            - secretRef:
                name: db-credentials

          env:
            - name: SHARED_SECRET
              valueFrom:
                secretKeyRef:
                  name: shared-secret
                  key: SHARED_SECRET

            - name: CORS_ALLOWED_ORIGIN
              value: "http://capstone-alb-127668070.eu-north-1.elb.amazonaws.com"

            - name: COOKIE_SECURE
              value: "false"

          readinessProbe:
            httpGet:
              path: /health
              port: 5101
            initialDelaySeconds: 3
            periodSeconds: 5

          livenessProbe:
            httpGet:
              path: /health
              port: 5101
            initialDelaySeconds: 10
            periodSeconds: 10

          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"